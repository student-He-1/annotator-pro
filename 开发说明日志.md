# Annotator Pro · 开发说明

v2.3 最终版。本地优先的图像标注工具，Tauri 2 + Svelte 5 + Python FastAPI GPU 推理。

> 个人学习练手，数据全部本地，不上传服务器。**本版本为最终版，不再迭代。**

---

## 一、快速使用

### 第 1 步：启动 Python 后端（必做）

```powershell
$env:TORCH_HOME="E:\TorchHub"
$env:PYTHONIOENCODING="utf-8"
& "D:\79458\Documents\anaconda3\envs\deeplearning\python.exe" "D:\桌面\图像标注\annotator-pro\src-tauri\target\release\sidecar\sam_server.py"
```

等控制台输出 `SAM_SERVER_READY http://127.0.0.1:1421`（约 10-30 秒，冷启动慢）。**这个窗口不要关。**

### 第 2 步：启动前端

双击 `src-tauri\target\release\annotator-pro.exe`

或浏览器开发模式见第三节。

### 前提条件

| 项 | 路径（写死在代码里） |
|---|---|
| Python | `D:\79458\Documents\anaconda3\envs\deeplearning\python.exe` |
| 后端脚本 | exe 同级 `sidecar\sam_server.py` |
| SAM3 模型 | `E:\Doubao download\sam3.pt` |
| YOLO pose 模型 | `E:\Doubao download\yolov8n-pose.pt` |
| face_alignment 权重 | `E:\TorchHub\hub\checkpoints\` |
| 环境变量 | `TORCH_HOME=E:\TorchHub` |

> 注：Tauri sidecar 自动拉起因模型冷启动超过 30 秒超时，已放弃。需手动起后端。

---

## 二、功能清单（v2.3）

### 标注工具
- 矩形框 `R`、多边形 `P`、旋转框 `O`、关键点 `K`、魔术棒 `W`、SAM `M`
- 选择 `V`，滚轮缩放，右键平移，撤销/重做

### AI 辅助
- **PCS 文本提示分割**：右侧属性栏输入文本（如 cat/狗）+ 阈值，一键分割全部实例
- **人体17点**：YOLOv8n-pose，COCO 骨架连线
- **人脸68点**：face_alignment 2DFAN4
- **SAM 点选分割**：点击物体出 mask
- **批量预标注**：右侧属性栏一键对全部/未标注图片跑 PCS/pose/face

### 导出
- PASCAL VOC XML、YOLO TXT、COCO JSON（带关键点 keypoints/skeleton）、CreateML、LabelMe
- **U-Net Mask**：按类别值填充灰度 PNG
- 可视化 PNG/JPG、图像分类 CSV

### 其他
- 自定义关键点模板（设置面板，存 localStorage）
- 标注统计、数据集划分、图片质量筛查
- 工程保存/加载 `.annotator`
- 深色/浅色主题、中英文

---

## 三、开发模式（浏览器调试）

```powershell
# 后端
cd "D:\桌面\图像标注\annotator-pro"
$env:TORCH_HOME="E:\TorchHub"
$env:PYTHONIOENCODING="utf-8"
& "D:\79458\Documents\anaconda3\envs\deeplearning\python.exe" python\sam_server.py

# 前端（另开一个终端）
$env:npm_config_cache="E:\TempCache\npm"
cd "D:\桌面\图像标注\annotator-pro"
npm run dev
```

浏览器开 http://localhost:1420

> 注意：当前工程已删除 node_modules，开发模式需先 `npm install`。

---

## 四、重新打包（如需）

```powershell
Stop-Process -Name "annotator-pro" -Force -ErrorAction SilentlyContinue
$env:npm_config_cache="E:\TempCache\npm"
cd "D:\桌面\图像标注\annotator-pro"
npm install          # node_modules 已删，需重装
npm run build
npm run tauri build
# 打包后复制 sidecar 脚本
Copy-Item "src-tauri\sidecar\sam_server.py" "src-tauri\target\release\sidecar\sam_server.py" -Force
```

产物：`src-tauri\target\release\annotator-pro.exe`

---

## 五、注意事项

1. **批量预标注交叉污染**：批量处理多张后切图，canvas 可能显示错误图片内容。删除错标即恢复。已知问题，不再修。
2. **首次启动慢**：加载 SAM3 模型约 10-30 秒，属正常。
3. **GPU 显存**：SAM3 + YOLO + face_alignment 共约 4-5GB 显存，6GB 显卡可用。
4. **标注自动保存**：存浏览器 localStorage，换机器/清缓存会丢，重要数据用「保存工程」导出 `.annotator`。
5. **模型文件不打包**：sam3.pt（3.2GB）等模型需自行准备，路径写死在 `sidecar/sam_server.py`。

---

## 六、后续可拓展方向（不做了）

- [ ] 原生拖拽文件进画布
- [ ] macOS/Linux 打包
- [ ] 标注进度工程级持久化（不依赖 localStorage）
- [ ] 框提示分割（box prompt）
- [ ] 多语言模型支持

> **本版本为最终版，不再迭代。**
